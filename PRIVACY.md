# 隐私说明

最后更新：2026-08-14

本项目是一个本地优先的公众号文章编辑工具。默认情况下，它不包含账号系统、数据库、统计脚本或云端文件存储。

## 本机保存的数据

当用户选择保存设置或创建文章时，浏览器会在当前网站地址的 Local Storage 或 Session Storage 中保存以下数据：

- API 服务商、接口地址、模型名和 API Key
- 当前文章的标题、正文、作者信息、插图和图片说明
- 历史文章快照
- 工作台皮肤、个人 IP 名称、副标题和头像

这些数据只保存在设置它们的设备和浏览器中；其他访问者、GitHub 仓库和本项目代码均无法读取。清除该网站的浏览器数据后，这些内容会被删除。

## 何时发送数据

点击「AI 改写后排版」时，工具会将口播稿、可选标题、作者信息和所选模型配置发送给你填写的 API 服务商，用于生成文章。

点击「测试连接」时，工具会将 API Key 和接口地址发送给你填写的 API 服务商，用于验证连接。

本地 Node 服务只在请求期间转发数据，不会将口播稿、API Key、文章、头像或历史记录写入文件、数据库或日志。模型服务商如何处理请求数据，受该服务商的隐私政策和你的账户设置约束。

## GitHub 与开源仓库

GitHub 不会读取浏览器 Local Storage，因此正常使用本工具不会把 API Key、历史文章或头像同步到仓库。

项目的 `.gitignore` 会忽略常见密钥文件、本地草稿、上传目录和导出目录。但如果你手动把密钥、文章或头像复制进源代码、说明文档或其他未被忽略的文件，它们仍可能被提交。提交前请使用 `git status` 检查暂存内容。

## 你的控制权

- 在「模型设置」中使用「删除已存密钥」可删除本浏览器保存的模型配置。
- 在「历史记录」中可逐条删除或清空文章快照。
- 可在浏览器的网站数据设置中删除本工具的全部本地数据。
- 可以随时更换或移除自己在 API 服务商处的密钥。

## 公开部署时的变化

当前版本以本地服务形式运行。若未来将其部署到公网，部署方必须更新本说明，明确所使用的平台、服务端日志策略、访问日志保留时间，以及是否存在额外的数据收集或传输。
